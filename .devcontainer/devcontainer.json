{
    "name": "spi-base",
    "build": { "dockerfile": "Dockerfile" },
    "extensions": [
      "sswg.swift-lang",
    ],
    "settings": {
      "lldb.library": "/usr/lib/liblldb.so"
    },
    //"forwardPorts": [8080, 5432, 6432],

    // Comment out to connect as root instead. More info: https://aka.ms/vscode-remote/containers/non-root.
	  // "remoteUser": "vscode"
}